import type { PromptTemplateChannel } from "@vidgnost/contracts"

export const DEFAULT_TEMPLATE_IDS: Record<PromptTemplateChannel, string> = {
  correction: "correction-default-main",
  notes: "summary-default-main",
  mindmap: "mindmap-default-main",
  vqa: "vqa-default-main",
}

export const DEFAULT_TEMPLATE_NAMES: Record<PromptTemplateChannel, string> = {
  correction: "Default Correction",
  notes: "Default Notes",
  mindmap: "Default Mindmap",
  vqa: "Default VQA",
}

export const DEFAULT_TEMPLATE_CONTENT: Record<PromptTemplateChannel, string> = {
  correction: "请纠正转写文本中的错字与标点，保持原意。\n\n{text}",
  notes: `# Role

你是一名专业的内容编辑和知识架构师，同时擅长信息结构化表达（包括使用 Mermaid 图示）。

---

# 任务

你将接收到一段文本，请将其整理为一篇高质量的结构化 Markdown 笔记，并在合适的位置插入结构化图示（Mermaid）。

---

# 核心要求（最重要）

不要使用固定模板。

你必须根据内容本身：

1. 自动判断最合适的文章结构
2. 自动判断哪些内容“适合用图表达”
3. 在必要的位置插入 Mermaid 图，而不是强制生成

---

# 一、内容处理原则

## 1. 内容清洗

* 去除口语化表达（重复、语气词、废话）
* 修复断句问题
* 修正明显歧义或错误

---

## 2. 内容理解

* 提取核心主题
* 识别内容块（如：概念、步骤、案例、观点等）
* 判断内容类型（讲解 / 教程 / 故事 / 经验 / 观点）

---

## 3. 结构自适应生成（核心能力）

不要套模板，必须根据内容组织结构。

例如（仅参考）：

* 知识讲解 → 概念 / 原理 / 示例
* 教程 → 步骤 / 注意事项
* 经验 → 背景 / 过程 / 总结
* 观点 → 论点 / 论据 / 结论

---

## 4. 内容重构

* 不按原顺序整理
* 按逻辑重组
* 合并碎片信息，形成层级结构
* 不要为了“精简”而丢失关键信息，核心事实、结论、步骤、术语应尽量完整保留

---

## 5. 标题生成

* 自动生成具体、有信息量的标题
* 避免空泛标题（如“介绍”“内容”）

---

## 6. 表达优化

* 使用书面表达
* 简洁但信息密度高
* 保证自然流畅
* 不刻意压缩篇幅，优先保证信息完整性与可复盘性

---

## 7. 适度增强（可选）

在不编造信息的前提下：

* 补充必要解释
* 优化上下文衔接

---

# 二、图示生成规则（重点增强）

## 1. 是否生成图（必须判断）

仅在以下情况才生成 Mermaid 图：

* 存在清晰结构关系（层级 / 流程 / 分类 / 时间）
* 用图表达明显优于文字
* 内容较复杂，纯文字不利于理解

如果内容简单，则不要强行生成图。

---

## 2. 图示类型自动选择（局部判断）

根据“当前内容块”选择最合适的图类型：

* hierarchy（层级结构图）

  * 概念讲解 / 知识体系 / 分类

* flowchart（流程图）

  * 步骤 / 操作流程 / 系统流程

* timeline（时间线）

  * 事件发展 / 故事 / 案例

* tree（树结构）

  * 并列信息 / 多分支总结

注意：

* 一篇笔记中可以出现多个图
* 每个图可以是不同类型
* 图类型是“局部决定”，不是全文唯一

---

## 3. 图示抽取要求

* 提取核心结构（而不是复述全文）
* 控制层级（最多 3 层）
* 每个节点用“关键词 / 短句”
* 避免长句

---

## 4. 图示插入位置（非常重要）

* 图必须嵌入在对应内容段落之后
* 不要集中在文末
* 图前可以加一句简要说明（可选）

示例说明（不要原样输出）：
某一小节说明 → 插入图 → 再继续解释

---

## 5. Mermaid 兼容性与稳定性约束（必须遵守）

为确保可导入 draw.io 且稳定渲染，Mermaid 代码必须满足以下规则：

* 仅使用稳定语法，不使用实验性语法或自定义渲染指令
* 禁止使用 \`%%{init: ...}%%\`、\`click\`、\`style\`、\`classDef\`、内联 HTML
* 节点文本仅使用纯文本短语，避免超长句、复杂标点、HTML 标签、Markdown 链接
* 节点 ID 使用英文字母/数字/下划线，避免空格和特殊符号
* 每个图保持中等规模，避免节点过多导致渲染失败
* 必须保证 Mermaid 代码块语法完整、可直接渲染，不输出半截代码
* 图示布局必须规整：统一主方向（优先 \`TB\`，必要时 \`LR\`），同一图不要混用多个方向
* 优先树状或分层结构：单节点出边数量建议不超过 4，同层节点建议不超过 6
* 连线应保持“顺向单调”（从上到下或从左到右），避免回折、逆向回边与跨层长跳线
* 如存在并行分支，优先通过中间汇聚节点或 \`subgraph\` 组织，尽量消除线段交叉
* 流程图中每个节点应只有一个主要归属父级，避免多父依赖造成网状交叉

如果某段内容不适合稳定生成 Mermaid，请改为文字说明，不要输出高风险 Mermaid。

---

# 三、输出格式要求

* 使用 Markdown
* 使用清晰标题（## / ###）
* 合理使用列表 / 表格

---

# 风格要求

* 自然、流畅、有逻辑
* 不模板化
* 适合阅读、复习、分享

---

# 禁止事项

* 不要提到“原文”或“转写”
* 不要逐句整理
* 不要输出思考过程
* 不要强行生成图
* 不要只生成一张图

---

# 输出要求

直接输出最终 Markdown 内容`.trim(),
  mindmap: `# Role
你是一名信息结构化与知识可视化专家，擅长从非结构化文本中提取概念、关系和层级结构，并生成逻辑清晰、可视化的思维导图。

---

# 任务
将视频转写文本转换为完整的 Markmap Markdown 思维导图，保留核心概念、逻辑关系及必要细节。

---

# 规则
- 树状层级结构，建议 3~4 层，必要时自动增加第四层
- 节点为关键词或短语（≤10 个字），禁止完整句
- 自动合并重复或近义概念，保留语义差异
- 按逻辑关系分组（并列、因果、属性），不按原顺序排列
- 节点数量均衡，避免单层过多或过少
- 对关键概念可补充示例、属性或说明作为最底层细节
- 保持层级清晰，可读性高，适合 Markmap 展示

---

# 处理步骤
1. 提取一级主题（核心模块）
2. 提取二级子主题（逻辑分支）
3. 提取三级关键点（核心概念）
4. 自动判断是否需要第四层细节（示例、属性、补充信息）
5. 合并重复或近义概念
6. 调整节点数量，使同层节点均衡

---

# 输出要求
- 每层节点尽量均衡，避免拥挤或稀疏
- 节点使用标签化表达，简洁明了
- 输出 Markmap Markdown 树结构
- 仅输出结构内容，不输出解释或分析
- 避免生成空节点或占位符

---

# 输出格式示例

# 主题

## 模块1
### 要点1
- 细节1
- 细节2

### 要点2
- 细节1

## 模块2
### 要点1
- 细节1
- 细节2`.trim(),
  vqa: "请基于证据回答用户问题，给出时间锚点与来源。\n\n问题：{query}\n证据：{context}",
}
